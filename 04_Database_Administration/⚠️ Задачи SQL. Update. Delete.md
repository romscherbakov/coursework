# SQL Операции: UPDATE и DELETE

## 📋 Задания для выполнения

**Задание 1**  
Перевести всех дизайнеров в отдел маркетинга

<details>
<summary>Подсказка</summary>

```sql
UPDATE workers SET department_id = 3 WHERE department_id = 2;
```
</details>

---

**Задание 2**  
Изменить название тега "саппорт" на "поддержка"

<details>
<summary>Подсказка</summary>

```sql
UPDATE tags SET name = 'поддержка' WHERE name = 'саппорт';
```
</details>

---

**Задание 3**  
Установить все задачи выполненными по проекту КайзерДом

<details>
<summary>Подсказка</summary>

```sql
UPDATE tasks SET done = 1 WHERE project_id = 1;
```
</details>

---

**Задание 4**  
Увеличить все выплаты после февраля на 200 единиц

<details>
<summary>Подсказка</summary>

```sql
UPDATE salaries SET salary = salary + 200 WHERE date > '2020-02-29';
```
</details>

---

**Задание 5**  
Изменить название проектов со словом Юнитраст, указав им в имени "(архив)"

<details>
<summary>Подсказка</summary>

```sql
UPDATE projects SET name = CONCAT(name, ' (архив)') WHERE name LIKE '%Юнитраст%';
```
</details>

---

**Задание 6**  
Удалить все задачи, которые были поставлены раньше февраля и были выполнены

<details>
<summary>Подсказка</summary>

```sql
DELETE FROM tasks WHERE created_at < '2020-02-01' AND done = 1;
```
</details>

---

**Задание 7**  
Удалить тег "кнопки"

<details>
<summary>Подсказка</summary>

```sql
DELETE FROM tags WHERE name = 'кнопки';
```
</details>

---

**Задание 8**  
Удалить все выплаты от марта, кроме сотрудника с id=11

<details>
<summary>Подсказка</summary>

```sql
DELETE FROM salaries WHERE MONTH(date) = 3 AND worker_id != 11;
```
</details>

---

**Задание 9**  
Удалить все задачи

<details>
<summary>Подсказка</summary>

```sql
DELETE FROM tasks;
```
</details>

---

**Задание 10**  
Удалить сотрудника "Суворов Алексей", а во всех его задачах вместо него указать "Белянин Алексей"

<details>
<summary>Подсказка</summary>

```sql
-- Сначала обновляем задачи
UPDATE tasks SET worker_id = 11 WHERE worker_id = 12;
-- Затем удаляем сотрудника
DELETE FROM workers WHERE id = 12;
```
</details>


---

## 🚨 ВАЖНОЕ ПРЕДУПРЕЖДЕНИЕ

### ⚠️ ОПАСНОСТЬ КОМАНД UPDATE И DELETE

Команды **UPDATE** и **DELETE** являются **деструктивными операциями** и могут привести к:

- **Безвозвратной потере данных**
- **Нарушению целостности базы данных**
- **Критическим сбоям в работе приложения**
- **Нарушению связей между таблицами**

### 🔒 Меры предосторожности:

1. **Всегда делайте BACKUP** базы данных перед выполнением
2. **Тестируйте запросы** на тестовой среде
3. **Используйте транзакции** для возможности отката
4. **Начинайте с SELECT** для проверки условий
5. **Проверяйте связи между таблицами** перед удалением

---

## 📚 Шпаргалка по SQL командам

### 🔄 Команда UPDATE

#### Базовый синтаксис:
```sql
UPDATE table_name 
SET column1 = value1, column2 = value2, ...
WHERE condition;
```


### 🗑️ Команда DELETE

#### Базовый синтаксис:
```sql
DELETE FROM table_name WHERE condition;
```

---

## 🛡️ Лучшие практики для вашей базы данных

### 1. Всегда проверяйте данные перед операциями
```sql
-- Перед обновлением дизайнеров
SELECT * FROM workers WHERE department_id = 2;

-- Перед удалением задач
SELECT COUNT(*) FROM tasks WHERE created_at < '2020-02-01' AND done = 1;

-- Перед обновлением проектов
SELECT * FROM projects WHERE name LIKE '%Юнитраст%';
```

### 2. Используйте транзакции для безопасности
```sql
START TRANSACTION;

-- Выполняем операции
UPDATE workers SET department_id = 3 WHERE department_id = 2;
UPDATE tags SET name = 'поддержка' WHERE name = 'саппорт';

-- Если всё правильно - подтверждаем
COMMIT;

-- Если что-то пошло не так - откатываем
-- ROLLBACK;
```

### 3. Обращайте внимание на внешние ключи
```sql
-- Перед удалением тега проверьте связи
SELECT * FROM tags_tasks WHERE tag_id = (SELECT id FROM tags WHERE name = 'кнопки');

-- Перед удалением сотрудника проверьте задачи
SELECT * FROM tasks WHERE worker_id = (SELECT id FROM workers WHERE name = 'Суворов Алексей');
```

---

## 📝 Сложные операции

### Для задачи №10 (замена сотрудника):

```sql
-- Шаг 1: Находим ID сотрудников
SELECT id FROM workers WHERE name = 'Суворов Алексей'; -- Предположим, ID = 12
SELECT id FROM workers WHERE name = 'Белянин Алексей'; -- Предположим, ID = 11

-- Шаг 2: Начинаем транзакцию
START TRANSACTION;

-- Шаг 3: Обновляем задачи где Суворов Алексей исполнитель
UPDATE tasks SET worker_id = 11 WHERE worker_id = 12;

-- Шаг 4: Обновляем задачи где Суворов Алексей создатель
UPDATE tasks SET creator_id = 11 WHERE creator_id = 12;

-- Шаг 5: Обновляем выплаты
UPDATE salaries SET worker_id = 11 WHERE worker_id = 12;

-- Шаг 6: Удаляем сотрудника
DELETE FROM workers WHERE id = 12;

-- Шаг 7: Проверяем результаты и коммитим
COMMIT;
```

### Альтернативный безопасный подход:
```sql
-- Вместо удаления помечаем сотрудника как неактивного (если есть такое поле)
ALTER TABLE workers ADD COLUMN active BOOLEAN DEFAULT TRUE;
UPDATE workers SET active = FALSE WHERE name = 'Суворов Алексей';
```

---

## 🔍 Проверочные запросы после выполнения

```sql
-- Проверка дизайнеров
SELECT * FROM workers WHERE department_id = 2;

-- Проверка тегов
SELECT * FROM tags WHERE name IN ('саппорт', 'поддержка');

-- Проверка задач проекта КайзерДом
SELECT * FROM tasks WHERE project_id = 1;

-- Проверка выплат
SELECT * FROM salaries WHERE date > '2020-02-29';

-- Проверка проектов
SELECT * FROM projects WHERE name LIKE '%Юнитраст%';

-- Проверка удаленных задач
SELECT * FROM tasks WHERE created_at < '2020-02-01' AND done = 1;

-- Проверка выплат за март
SELECT * FROM salaries WHERE MONTH(date) = 3;
```

---

## 🎯 Структура нашей базы данных

### Основные таблицы:
- **workers** - сотрудники (связана с departments)
- **departments** - отделы
- **tasks** - задачи (связана с workers, projects)
- **projects** - проекты
- **salaries** - выплаты (связана с workers)
- **tags** - теги
- **tags_tasks** - связь тегов и задач

---

## 💡 Рекомендации по выполнению

1. **Выполняйте операции по порядку** - некоторые операции могут зависеть от других
2. **Особое внимание на операцию №10** - она затрагивает несколько таблиц
3. **Операция №9 (удаление всех задач)** - очень опасна, выполнять последней
4. **Сделайте полный дамп базы** перед началом работ

---

**Удачи в выполнении заданий! 🚀**

