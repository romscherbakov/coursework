# 📊 Практическое задание: Агрегирующие запросы в SQL

## 🎯 Цель задания
Освоить работу с операторами `DISTINCT`, `GROUP BY` и агрегирующими функциями (`COUNT`, `SUM`, `AVG`) в различных комбинациях.

## 📚 Изучаемые операторы
- ✅ `GROUP BY`
- ✅ `GROUP BY + COUNT`
- ✅ `GROUP BY + SUM`
- ✅ `GROUP BY + WHERE`
- ✅ `GROUP BY + HAVING`
- ✅ `GROUP BY Multiple`

## 🗃️ Структура базы данных
Работаем с базой `departments`, которая содержит таблицы:
- `departments` - отделы компании
- `workers` - сотрудники
- `salaries` - зарплаты
- `tasks` - задачи
- `tags` - теги
- `tags_tasks` - связь тегов и задач
- `projects` - проекты

---

## 📝 Основные задания

1. **Подсчитать суммы зарплат по всем сотрудникам**
2. **Вывести средние суммы зарплат сотрудников до марта от больших к меньшим**
3. **Вывести средние суммы зарплат по месяцам**
4. **Вывести суммы всех зарплат по месяцам в которых суммы зарплат больше или равны 2000**
5. **Подсчитать количество тегов по каждой задаче и вывести в порядке убывания количества**
6. **Выбрать идентификатор второго по популярности тега**
7. **Для каждого сотрудника кроме worker_id=11,12 вывести количество завершенных задач**
8. **Вывести по месяцу и дню в месяце сумму зарплат**

---

## 🧠 Дополнительные задания (GROUP BY + HAVING)

9. **Найти сотрудников, у которых было более 2 выплат зарплаты**
10. **Вывести месяцы, в которых средняя зарплата по всем выплатам была больше 400**
11. **Найти сотрудников, у которых общая сумма выплаченных зарплат превышает 800**
12. **Найти сотрудников, которые выполнили более 2 задач**

---

## 💡 Подсказки
- Используйте `DISTINCT` для получения уникальных значений
- `GROUP BY` группирует данные по указанным столбцам
- `HAVING` фильтрует результаты группировки (аналог `WHERE` для `GROUP BY`)
- Агрегирующие функции: `COUNT()`, `SUM()`, `AVG()`, `MAX()`, `MIN()`
- Для работы с датами используйте `YEAR()`, `MONTH()`, `DAY()`

---

**Удачи в выполнении заданий! 🚀**
