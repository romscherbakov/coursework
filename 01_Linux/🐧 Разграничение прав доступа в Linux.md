# 🐧 Практическая работа: Разграничение прав доступа в Linux

## 📚 Цель работы
Освоить механизмы разграничения прав доступа к файлам и директориям в Linux, понять принципы работы с правами для пользователей и групп.

## 🎯 Задачи
- Изучить систему прав доступа в Linux
- Научиться управлять правами через `chmod`, `chown`, `chgrp`
- Понять разницу между правами на файлы и директории
- Освоить специальные права (sticky bit, setuid, setgid)
- На практике создать структуру с разными уровнями доступа

## ⚙️ Подготовка
**Сервер:** Ubuntu Linux  
**Пользователи:** Каждому студенту создан аккаунт (student1, student2, ...)  
**Рабочая директория:** `/opt/labs/`

---

## 📝 Теоретическая часть

### 🔐 Основные права доступа

| Право | Файлы | Директории |
|-------|-------|------------|
| **r (read)** | Чтение содержимого | Просмотр списка файлов |
| **w (write)** | Изменение содержимого | Создание/удаление файлов |
| **x (execute)** | Запуск как программы | Доступ в директорию |

### 👥 Владельцы и группы

| Обозначение | Описание |
|-------------|----------|
| **u** (user) | Владелец файла |
| **g** (group) | Группа-владелец |
| **o** (others) | Все остальные пользователи |
| **a** (all) | Все категории |

---

## 🛠️ Практическая часть

### 📁 Задание 1: Создание рабочей структуры

<details>
<summary>🔧 Команды для выполнения</summary>

```bash
# Переходим в рабочую директорию
cd /opt/labs/

# Создаем свою директорию (замени X на свой номер)
sudo mkdir -v studentX
sudo chown -v studentX:studentX studentX

# Входим в свою директорию и проверяем, что мы именно там где нужно
cd studentX
pwd

# Создаем поддиректории
mkdir -v public private restricted shared
```
</details>

### 🔒 Задание 2: Настройка базовых прав

<details>
<summary>🎯 Задача: Настроить разные уровни доступа</summary>

```bash
# 1. public - доступен всем для чтения и записи
chmod -v 777 public

# 2. private - доступен только владельцу
chmod -v 700 private

# 3. restricted - доступен для чтения всем, но писать может только владелец
chmod -v 755 restricted

# 4. shared - группа может читать и писать, остальные - только читать
chmod -v 775 shared

# Проверяем права
ls -la
```
</details>

### 📄 Задание 3: Работа с файлами

<details>
<summary>📝 Создаем тестовые файлы</summary>

```bash
# Создаем файлы в каждой директории
echo "Public file" > public/file_public.txt
echo "Private file" > private/file_private.txt  
echo "Restricted file" > restricted/file_restricted.txt
echo "Shared file" > shared/file_shared.txt

# Проверяем права файлов
ls -l public/ private/ restricted/ shared/
```
</details>

### 🔍 Задание 4: Тестирование доступа

<details>
<summary>🧪 Проверяем работу прав</summary>

```bash
# Пробуем прочитать файлы другого студента (замени Y на номер другого студента)
cat /opt/labs/studentY/public/file_public.txt
cat /opt/labs/studentY/private/file_private.txt
cat /opt/labs/studentY/restricted/file_restricted.txt

# Пробуем записать в чужие директории
echo "test" >> /opt/labs/studentY/public/test.txt
echo "test" >> /opt/labs/studentY/restricted/test.txt

# Пробуем удалить чужие файлы
rm -v /opt/labs/studentY/public/file_public.txt
```
</details>

### 🎪 Задание 5: Sticky Bit для общей директории

<details>
<summary>📌 Создаем защищенную общую директорию</summary>

```bash
# Создаем общую директорию (выполняет преподаватель)
sudo mkdir -v /opt/labs/shared_all
sudo chmod -v 1777 /opt/labs/shared_all

# Каждый студент создает свой файл
echo "File by studentX" > /opt/labs/shared_all/studentX_file.txt

# Пробуем удалить файл другого студента
rm -v /opt/labs/shared_all/studentY_file.txt

# Смотрим права директории
ls -ld /opt/labs/shared_all
```
</details>

### 👥 Задание 6: Работа с группами

<details>
<summary>👨‍👩‍👧‍👦 Создаем групповой доступ</summary>

```bash
# Создаем группу (выполняет преподаватель)
sudo groupadd lab_group

# Добавляем студентов в группу
sudo usermod -aG lab_group studentX
sudo usermod -aG lab_group studentY

# Создаем групповую директорию
sudo mkdir -v /opt/labs/group_work
sudo chgrp lab_group /opt/labs/group_work
sudo chmod -v 770 /opt/labs/group_work

# Проверяем доступ
ls -ld /opt/labs/group_work
groups studentX
```
</details>

### ⚡ Задание 7: SetGID для групповой работы

<details>
<summary>🔗 Настраиваем наследование группы</summary>

```bash
# Устанавливаем setgid на групповую директорию
sudo chmod -v 2770 /opt/labs/group_work

# Создаем файлы в директории
echo "Group file 1" > /opt/labs/group_work/file1.txt
echo "Group file 2" > /opt/labs/group_work/file2.txt

# Проверяем группу у созданных файлов
ls -l /opt/labs/group_work/
```
</details>

---

## 🔬 Контрольные вопросы

### ❓ Вопрос 1
Чем отличаются права `755` и `750` для директории?

<details>
<summary>💡 Ответ</summary>

- `755`: Владелец - полный доступ, группа и остальные - чтение и выполнение
- `750`: Владелец - полный доступ, группа - чтение и выполнение, остальные - нет доступа
</details>

### ❓ Вопрос 2
Что произойдет, если у файла убрать право на выполнение (x)?

<details>
<summary>💡 Ответ</summary>

Файл нельзя будет запустить как программу или скрипт, даже если он исполняемый.
</details>

### ❓ Вопрос 3
Для чего нужен sticky bit?

<details>
<summary>💡 Ответ</summary>

Sticky bit разрешает удалять файлы в директории только их владельцам, даже если у других пользователей есть право записи в эту директорию.
</details>

### ❓ Вопрос 4
В чем разница между `chmod +x` и `chmod u+x`?

<details>
<summary>💡 Ответ</summary>

- `chmod +x` - добавляет право выполнения для всех (user, group, others)
- `chmod u+x` - добавляет право выполнения только для владельца
</details>

---

## 📊 Таблица прав доступа

### Числовые обозначения прав:

| Права | Число | Буквы |
|-------|-------|-------|
| --- | 0 | 000 |
| --x | 1 | 001 |
| -w- | 2 | 010 |
| -wx | 3 | 011 |
| r-- | 4 | 100 |
| r-x | 5 | 101 |
| rw- | 6 | 110 |
| rwx | 7 | 111 |

### Часто используемые комбинации:

| Назначение | Файлы | Директории |
|------------|-------|------------|
| Только владелец | 600 | 700 |
| Владелец + группа | 660 | 770 |
| Чтение всем | 644 | 755 |
| Полный доступ всем | 666 | 777 |
| Безопасная общая | - | 1777 |

---

## ✅ Критерии оценки

- **✅ Создана структура директорий** (4 поддиректории с разными правами)
- **✅ Настроены корректные права доступа** 
- **✅ Протестирован доступ между студентами**
- **✅ Работа с sticky bit и setgid**
- **✅ Ответы на контрольные вопросы**

## 📖 Дополнительные материалы

- `man chmod` - справка по команде chmod
- `man chown` - справка по команде chown  
- `man umask` - настройка прав по умолчанию

---

**🎉 Успешной работы! Не забывайте проверять права после каждой операции!**
