# Лабораторная работа №1
## Изучение базовых команд Linux

### 📋 Цель задания
Освоить основы работы в терминале операционной системы Linux, изучить основные команды для управления файлами и каталогами, а также понять разницу между типами ссылок.

### ⚙️ Условия выполнения
- **Формат выполнения**: индивидуально или в группе
- **Доступ**: SSH-подключение к серверу Linux (это та машина, на которой установлена Ubuntu)
- **Исходные данные**:
  - Логин: `vboxuser`
  - Пароль: `changeme`
  - IP-адрес Ubuntu Linux 
 

---

## 🚀 Часть I. Подключение к серверу и создание пользователя

### 1. Подключение к серверу
```bash
ssh vboxuser@server_address
```

### 2. Создание пользователя
```bash
sudo useradd -m studentX -s /bin/bash
```
*где `X` - номер рабочего места*

### 3. Добавление в группу sudo
```bash
sudo usermod -aG sudo studentX
```

### 4. Установка пароля
```bash
sudo passwd studentX
```

### 5. Ограничение срока действия учетной записи
```bash
sudo chage -E 2025-12-01 studentX
```

---

## 🔧 Подготовка рабочей среды

### 6. Переподключение под новым пользователем
```bash
exit

ssh studentX@server_address
```

> **⚠️ Внимание**: Работайте под пользователем `studentX`, не переключайтесь на `root`. Для административных задач используйте `sudo`.

### 7. Настройка истории команд
```bash
cd /home/studentX
nano .bashrc
```

**Добавьте в конец файла:**
```bash
export HISTTIMEFORMAT="%d/%m/%y %T "
```

**Примените изменения:**
```bash
source ~/.bashrc
```

### 8. Проверка истории команд
```bash
history
```
*Убедитесь, что команды отображаются с временными метками*

---

## 💻 Часть II. Работа с базовыми командами Linux

### 9. Изучение команды `pwd`
```bash
man pwd
pwd
```

### 10. Изучение команды `cd`
```bash
man cd
cd /var/log
pwd

cd /etc/
pwd

cd /
pwd
```

### 11. Изучение команды `ls`
```bash
man ls
ls
ls -la
ls -lt
ls -lh
```

**В чем разница этих команд?**
- `ls` - базовый список файлов
- `ls -la` - подробный список со скрытыми файлами
- `ls -lt` - сортировка по времени изменения
- `ls -lh` - человеко-читаемые размеры файлов

### 12. Возврат в домашний каталог
```bash
cd
pwd
```

### 13. Создание каталога
```bash
man mkdir
mkdir -v test
```

### 14. Навигация и просмотр
```bash
cd test
pwd
ls -la
```

### 15. Создание вложенного каталога
```bash
mkdir -v test2
```

### 16. Создание пустого файла и заполнение его данными
```bash
man touch
touch test2/text
echo "123456" > test2/text
```

### 17. Переименование файла
```bash
man mv
mv -v test2/text test2/textSIT
```

### 18. Копирование файлов
```bash
man cp
cp -v test2/textSIT test2/copy.txt
```

### 19. Работа со ссылками
```bash
man ln
# Жесткая ссылка
ln test2/copy.txt hardlink
# Символическая ссылка
ln -s test2/copy.txt simlink
```

### 20. Просмотр результатов
```bash
ls -la
```

### 21. Проверка работы ссылок
```bash
cat hardlink
cat simlink
```

---

## 🔍 Часть III. Исследование поведения ссылок

### 22. Удаление исходного файла
```bash
rm -v test2/copy.txt
```

### 23. Проверка состояния ссылок
```bash
ls -la
```

### 24. Тестирование ссылок после удаления
```bash
cat hardlink
cat simlink
```

> **📸 Сделайте скриншоты результатов**

### 25. Анализ различий
*Зафиксируйте наблюдения за поведением разных типов ссылок*

### 26. Очистка
```bash
rm -rfv test2 hardlink simlink
cd ..
rmdir -v test
```

---

## 📝 Контрольные вопросы

### 1. Разница между жесткой и символической ссылкой

| **Аспект** | **Жесткая ссылка** | **Символическая ссылка** |
|------------|-------------------|-------------------------|
| **inode** | Совпадает с исходным файлом | Имеет собственный inode |
| **Файловая система** | Только в пределах одной ФС | Может ссылаться между разными ФС |
| **Каталоги** | Не поддерживает ссылки на каталоги | Поддерживает ссылки на каталоги |
| **Удаление оригинала** | Файл остается доступен | Ссылка становится "битой" |

### 2. Поведение жесткой ссылки при удалении исходного файла
**Жесткая ссылка продолжает работать** - данные остаются доступны, так как жесткая ссылка является альтернативным именем для того же самого inode. Файл будет окончательно удален только когда количество жестких ссылок станет равным нулю.

### 3. Поведение символической ссылки при удалении исходного файла
**Символическая ссылка становится "битой"** - при попытке доступа возникает ошибка "No such file or directory", так как символическая ссылка содержит только путь к целевому файлу.

### 4. Практическое применение каждого типа ссылок

**Символические ссылки идеально подходят для:**
- Создания ярлыков между разными разделами диска
- Организации удобных путей к часто используемым файлам
- Управления версиями (например, `latest → v2.3`)
- Создания временных ссылок на конфигурационные файлы

**Жесткие ссылки эффективны для:**
- Экономии места при хранении идентичных файлов
- Защиты от случайного удаления важных данных
- Создания мгновенных "копий" без дублирования данных
- Резервного копирования в пределах одной файловой системы

---

## 💡 Вывод
В ходе лабораторной работы были освоены базовые команды Linux для работы с файловой системой, изучены принципы создания и управления файлами и каталогами, а также экспериментально подтверждены различия в поведении жестких и символических ссылок при удалении исходных файлов.
```


