# 🐧 Лабораторная работа 4: Основы работы с файлами и текстовыми процессорами в Linux

<div align="center">

![Linux](https://img.shields.io/badge/Linux-FCC624?style=for-the-badge&logo=linux&logoColor=black)
![Bash](https://img.shields.io/badge/Bash-4EAA25?style=for-the-badge&logo=gnu-bash&logoColor=white)
![Terminal](https://img.shields.io/badge/Terminal-000000?style=for-the-badge&logo=windowsterminal&logoColor=white)

**Освоение базовых операций работы с файлами в Linux**

</div>

## 🎯 Цель работы

Ознакомиться с базовыми операциями работы с файлами в Linux: создание и редактирование файлов с помощью текстовых редакторов, а также поиск и обработка текста с помощью утилит `grep`, `cut`, `sed` и `find`.

## ⚡ Подготовка к работе

### 🔧 Настройка окружения

```bash
# 1. Запустите MobaXterm
# 2. Подключитесь к серверу по SSH:
ssh studentX@192.168.1.22
# (где X - ваш номер)

# Введите ваш пароль при запросе (ввод символов не отображается).
```
Переходим в свою директорию на сервере `/opt/labs/studentX` и уже в ней проводим всю работу
```bash
cd /opt/labs/studentX

# где X - это ваш номер
```

📝 Часть 1: Работа с текстовыми редакторами

Цель: Научиться создавать и редактировать текстовые файлы.

🖊️ Задание 1.1: Редактор Nano
Nano — простой и интуитивно понятный редактор.

```bash
# Создайте файл nano_test.txt:
nano nano_test.txt
```
Действия:

Напишите 3-4 строки о своем любимом хобби, фильме и т.п.

Сохранение: Ctrl+O (Write Out), нажмите Enter для подтверждения имени файла

Выход: Ctrl+X
Провека, что файл создан:
```bash
ls -la 
```

⌨️ Задание 1.2: Редактор vi/vim
vi — более мощный, но сложный редактор.

```bash
# Создайте файл vi_test.txt:
vi vi_test.txt
```
Команды редактора vi (иногда это может быть единственный редактор в ОС):

Действие	Команда:
* Переход в режим вставки	i
* Написание текста	(просто печатайте)
* Выход из режима вставки	Esc
* Сохранение и выход	:wq + Enter   (да, такая команда : - двоеточние, w - write, q - quit)
* Выход без сохранения	:q!
  
💡 Совет: Если вы что-то не так отредактировали, чтобы ничего не сохранять - делаем выход без сохранения (используйте :q! )

📊 Часть 2: Создание файла с данными

Цель: Создать структурированный файл для последующих заданий.

Создайте файл students.txt с помощью редактора `vi` со следующим содержимым:

```text
Имя Фамилия Группа Возраст Город
Иван Петров P3210 20 Москва
Мария Сидорова P3210 19 Санкт-Петербург
Петр Иванов P3209 21 Казань
Анна Козлова P3209 20 Москва
Алексей Смирнов P3210 22 Новосибирск
```

🔍 Часть 3: Поиск текста с помощью `grep`
Цель: Научиться искать строки по шаблону.

📋 Примеры использования `grep`.

Попробуйте выполнить данные команды с файлом students.txt
```bash
grep "Москва" students.txt	  #Найти строки с "Москва"

grep -n "P3210" students.txt	#Найти с номерами строк

grep -i "петр" students.txt	  #Игнорировать регистр

grep -A1 "Анна" students.txt	 # Показать контекст (очень полезная команда для чтения лог-файлов, файлов журналов)
```

🎯 Задание 3.1

 1. Найдите всех студентов из группы P3209
 2. Найдите всех студентов, чьё имя начинается на букву "А" и сохраните результат команды в файл `grep_result.txt`
<details>
<summary>Подсказка к заданию 2</summary>

```bash
grep "шаблон" файл.txt > новый_файл.txt
в шаблоне можно использовать символ ^, он соответствует пустой строке в начале и конце строки соответственно
```
</details>

 3. Найдите всех студентов, кто не из "Москвы" и результатом команды дополните файл `grep_result.txt`
<details>
<summary>Подсказка к заданию 3</summary>

```bash
grep "шаблон" файл.txt >> новый_файл.txt
символ >> - мы дописываем данные в файл, добавляем вывод нашей команды в конец новый_файл.txt
также используем:
-v, --invert-match - чтобы выбрать несовпадающие строки
```
</details>

 4. Найдите строку с "Анна" с контекстом и сохраните в `grep_context.txt`
<details>
<summary>Подсказка к заданию 4</summary>

  Нужно вывести три строки: одна строку сверху, строку с шаблоном "Анна" и одну строку снизу.

```text
Петр Иванов P3209 21 Казань
Анна Козлова P3209 20 Москва
Алексей Смирнов P3210 22 Новосибирск

 Чтобы вывести контекст читаем: man grep
ищем раздел Context Line Control
```

</details>

 5. Дополнительно: Найдите все строки с "P3210" и покажите 2 строки контекста после

✂️ Часть 4: Вырезка частей строк с помощью `cut`

Цель: Научиться извлекать конкретные столбцы из текста.

📋 Примеры cut
```bash
# Извлечь первый столбец (имена)
cut -d' ' -f1 students.txt

# Извлечь имена и города (столбцы 1 и 5)
cut -d' ' -f1,5 students.txt
```

🎯 Задание 4.1

1. Используя файл `students.txt` cоздайте файл `cities.txt` с названиями городов  
2. Используя файл `students.txt` извлеките номера групп в файл `groups.txt`


🔄 Часть 5: Потоковый редактор sed

Цель: Понять основы поиска и замены текста.

📋 Примеры sed
```bash
# Заменить "Москва" на "МСК"
sed 's/Москва/МСК/g' students.txt

# Удалить строки с "Новосибирск"
sed '/Новосибирск/d' students.txt
```
🎯 Задание 5.1

 1. Создайте копию файла `students.txt`, назовите копию `students_sed.txt`
 2. Замените все вхождения "Петр" на "Пётр" в файле `students_sed.txt`
 3. Удалите группу P3209 и сохраните в файл `only_p3210.txt`

📁 Часть 6: Поиск файлов с помощью find

Цель: Научиться находить файлы и директории.

📋 Примеры find
```bash
# Найти .txt файлы (3 уровня глубины), ошибки не выводим, а отпраялем в /dev/null 
find . -maxdepth 3 -name "*.txt" -ls 2>/dev/null

# Найти файлы, измененные за последние 2 дня
find ~ -mtime -2 -ls 2>/dev/null
```
🎯 Задание 6.1

 Подготовка: 
 Создайте в /opt/labs/student`X` (где X - ваш номер) текстовый файл `my_find.txt`
  пусть содержимое файла `my_find.txt` будет "Это тестовый файл для поиска"

 1. Найдите все .txt файлы в /opt/labs/
 2. Найдите файл my_find.txt и сохраните путь в find_result.txt
    
 3. Найдите все файлы в /opt/labs/ размером больше 1КБ и сохраните список в size_result.txt
    
    `man find`
    ищем опцию `-size` и как она работает

 5. Найдите все файлы в /opt/labs/ измененные за последние 2 дня и сохраните список в recent_files.txt
    
    `man find`
    ищем опцию `-mtime` и как она работает

---
⚠️ ВАЖНЫЕ ПРЕДУПРЕЖДЕНИЯ
🚨 БЕЗОПАСНОСТЬ ПРЕВЫШЕ ВСЕГО!

✅ Всегда ограничивайте глубину поиска с помощью -maxdepth N

✅ Используйте 2>/dev/null для скрытия ошибок доступа

❌ НИКОГДА не выполняйте `find / -delete` - это удалит все файлы в системе! (также как и команду `rm -f / ` - классическая шутка для новичков)

✅ Всегда проверяйте команду с -ls перед деструктивными операциями

---

📊 Итоговый отчет

📁 Файлы для проверки:
```bash
# Вывод команды ls -la
ls -la
```
📄 Содержимое файлов для предоставления преподавателю:

* `grep_result.txt` - результат поиска по возрасту с цифрой 0

* `grep_context.txt` - контекстный поиск "Анна"

* `cities.txt` - список городов

* `groups.txt` - номера групп

* `only_p3210.txt` - студенты только группы P3210

* `find_result.txt` - путь к найденному файлу
  
* `size_result.txt` - найденные файлы с размером больше 1 Kb
  
* `recent_files.txt` - найденные файлы, измененные за последние 2 дня

❓ Вопрос для размышления:
"Какой редактор (nano или vi) вам показался удобнее и почему?"


🚀 Удачи в выполнении задания!
Помните: практика - ключ к освоению Linux! ⚡





