# Лабораторная работа №2: Углубленное изучение базовых команд Linux

## Цель работы
Закрепить навыки работы в командной строке Linux, научиться управлению файловой системой, практическому использованию различных типов ссылок и анализу их поведения.

## Критерии оценки
- Точность выполнения команд
- Понимание выводимых результатов
- Способность анализировать поведение файловой системы и ссылок

---

## 📁 Часть I: Подготовка рабочего пространства

### 1. Подключение к серверу
```bash
ssh studentX@server_address
```

### 2. Определение текущего местоположения
```bash
pwd
```

### 3. Переход в домашний каталог
```bash
cd
```

### 4. Создание каталога для лабораторной работы
```bash
mkdir -v lab2_advanced && cd lab2_advanced
```

### 5. Подтверждение текущего местоположения
```bash
pwd
```

---

## 🛠️ Часть II: Создание сложной структуры каталогов и файлов

### 6. Создание структуры каталогов
**Требуемая структура:**
```
lab2_advanced/
├── projects/
│   └── web_app/
│       ├── backend/
│       └── frontend/
└── data/
    └── logs/
```

<details>
<summary><strong>🔧 Решение (только если не получается)</strong></summary>

```bash
mkdir -p -v projects/web_app/{backend,frontend} data/logs
```
</details>

### 7. Проверка созданной структуры
```bash
ls -R
```

**Дополнительная команда:**
```bash
tree
```

### 8. Создание тестовых файлов

<details>
<summary><strong>🔧 Подсказка по созданию файлов</strong></summary>

```bash
touch projects/web_app/backend/server.py
touch projects/web_app/frontend/index.html
echo "Log file started" > data/logs/app.log
touch README.md
```
</details>

---

## 🔗 Часть III: Практика с жесткими и символическими ссылками

### 9. Создание жесткой ссылки
```bash
ln data/logs/app.log hard_link.log
```

### 10. Создание символической ссылки
```bash
ln -s projects/web_app/ symlink_to_project
```

### 11. Проверка созданных ссылок
```bash
ls -la
```

### 12. Изучение информации о файлах
```bash
ls -li data/logs/app.log hard_link.log
```

**Дополнительные команды:**
```bash
stat hard_link.log
stat data/logs/app.log
stat symlink_to_project
```

---

## 🌀 Часть IV: Навигация и проверка ссылок

### 13. Навигация через символическую ссылку
```bash
cd symlink_to_project/frontend
pwd
```

### 14. Возврат в основной каталог
```bash
cd ../..
```

### 15. Добавление записи через жесткую ссылку
```bash
echo "New log entry" >> hard_link.log
```

### 16. Проверка содержимого исходного файла
```bash
cat data/logs/app.log
```

---

## 🧪 Часть V: Исследование поведения ссылок при удалении

### 17. Удаление исходного файла
```bash
rm -v data/logs/app.log
```

### 18. Проверка состояния ссылок
```bash
ls -la
cat hard_link.log
ls -l symlink_to_project
```

### 19. Анализ результатов
- **Жесткая ссылка (`hard_link.log`):** Доступно ли ее содержимое? Почему?
- **Символическая ссылка (`symlink_to_project`):** Изменился ли ее цвет? Что означает этот цвет? Работоспособна ли ссылка?

---

## 🧹 Часть VI: Расширенная очистка и проверка

### 20. Попытка удаления каталога
```bash
rmdir projects
```

### 21. Полное удаление рабочего каталога

<details>
<summary><strong>⚠️ Внимание: опасная операция!</strong></summary>

```bash
cd ..
rm -rfv lab2_advanced
```
</details>

---

## 📝 Контрольные вопросы

1. В чем основное различие между жесткой и символической ссылкой с точки зрения файловой системы?
2. Что произойдет с символической ссылкой, если переместить исходный файл/каталог в другое место внутри той же файловой системы?
3. Можно ли создать жесткую ссылку на каталог? Почему?
4. Почему команда `rmdir projects` в шаге 20 не выполнилась? Какую команду нужно было использовать?
5. Объясните, что означает вывод `ls -l` для битого (неработающего) симлинка.

---

## 📝 Контрольные ответы

### Сравнение жестких и символических ссылок в Linux

| Характеристика | Жесткая ссылка | Символическая ссылка |
|----------------|----------------|---------------------|
| **Inode** | Совпадает с исходным файлом | Имеет собственный inode |
| **Размер** | Не занимает дополнительное место | Занимает место (хранит путь) |
| **Природа** | Альтернативное имя того же файла | Указатель на путь к файлу |
| **Файловая система** | Должна быть той же | Может ссылаться между разными ФС |
| **Удаление оригинала** | Файл остается (пока есть ссылки) | Становится битой (нерабочей) |

### Поведение символических ссылок при перемещении файлов

#### Если переместить исходный файл/каталог
```bash
# Создание файла и симлинка
echo "test" > original_file.txt
ln -s original_file.txt symbolic_link.txt

# Перемещение исходного файла
mv -v original_file.txt /tmp/moved_file.txt
```

**Результат:**
- `symbolic_link.txt` → становится БИТОЙ ссылкой
- `symbolic_link.txt` продолжает указывать на "original_file.txt", которого нет

### Анализ битого симлинка

**Вывод `ls -l` для битого симлинка:**
```bash
$ ls -l broken_symlink
lrwxrwxrwx 1 user group 15 Jan 15 12:00 broken_symlink -> nonexistent_file
```

**Расшифровка вывода:**
- `l` - тип файла "symbolic link"
- `rwxrwxrwx` - права доступа (777, но не имеют практического значения)
- `1` - количество ссылок
- `user:group` - владелец и группа
- `15` - размер симлинка (длина строки пути)
- `Jan 15 12:00` - время создания симлинка
- `broken_symlink -> nonexistent_file` - имя симлинка и целевой путь

**Визуальный признак:** в терминале битые симлинки обычно подсвечиваются красным

**Проверка статуса симлинка:**
```bash
# Проверить, битый ли симлинк
file symbolic_link.txt
# Вывод: symbolic_link.txt: broken symbolic link to 'nonexistent_file'

# Узнать куда ссылается
readlink symbolic_link.txt
# Вывод: nonexistent_file

# Проверить существование целевого файла
ls -la $(readlink symbolic_link.txt)
```

---

**Примечание:** Будьте осторожны с командой `rm -rf` - она необратимо удаляет файлы!

